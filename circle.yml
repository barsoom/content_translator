dependencies:
  pre:
    - script/ci/prepare.sh
  cache_directories:
    - ~/dependencies
    - ~/.mix
    - _build
    - deps

test:
  override:
    - script/ci/tests.sh

deployment:
  production:
    branch: master
    commands:
      - script/ci/deploy.sh
